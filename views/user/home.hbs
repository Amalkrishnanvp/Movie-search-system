<section class="relative bg-gray-900 text-white min-h-screen flex items-center justify-center px-4">
    <!-- Background image -->
    <div class="absolute inset-0">
        <img src="/images/heroImage.jpg" alt="Movie Background" class="w-full h-full object-cover opacity-40">
    </div>

    <!-- Hero content -->
    <div class="relative z-10 text-center animate-fadeIn">
        <h1 class="text-4xl md:text-6xl font-bold mb-6">Find Your Favorite Movies</h1>
        <p class="text-lg md:text-xl mb-8 text-gray-300">Search and discover movies instantly</p>
        <div class="flex justify-center">
            <input type="text" placeholder="Search for a movie..."
                class="w-72 md:w-96 p-3 rounded-l-full text-black focus:outline-none">
            <button class="bg-blue-600 hover:bg-blue-700 p-3 rounded-r-full">
                Search
            </button>
        </div>
    </div>
</section>

{{!-- <section>
    <div class="mx-auto max-w-2xl px-4 py-8 sm:px-6 lg:max-w-7xl lg:px-8">
        <div class="grid grid-cols-2 gap-x-4 gap-y-6 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 xl:gap-x-6">
            {{#if movies.length}}
            {{#each movies}}
            <div
                class="flex flex-col h-full bg-white border border-gray-200 rounded-lg shadow-sm dark:bg-gray-800 dark:border-gray-700">
                <!-- Image container with centered image -->
                <div class="h-44 flex items-center justify-center bg-gray-100 dark:bg-gray-900 overflow-hidden">
                    <img class="max-h-full max-w-full object-scale-down"
                        src="https://image.tmdb.org/t/p/original{{this.PosterPath}}" alt="{{this.Title}} poster" />
                </div>
                <!-- Compact content area -->
                <div class="p-4 space-y-1">
                    <div class="flex justify-between items-start">
                        <h5 class="text-sm font-bold text-gray-900 dark:text-white line-clamp-2">
                            {{this.Title}}
                        </h5>
                        <span class="text-xs text-gray-500 dark:text-gray-400 ml-2 whitespace-nowrap">
                            {{this.Type}}
                        </span>
                    </div>
                    <p class="text-xs text-gray-700 dark:text-gray-400">{{this.Year}}</p>
                </div>
                <!-- Button area with Detailed View and Favorite button -->
                <div class="p-4 mt-auto flex justify-between items-center">
                    <a href="" id="add-movie-btn" data-id="{{this.imdbID}}" onclick="addMovie(event, '{{this.imdbID}}')"
                        class="inline-flex items-center px-3 py-2 text-xs font-medium text-center text-white bg-blue-700 rounded-md hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
                        Detailed View
                        <svg class="rtl:rotate-180 w-3 h-3 ms-1" aria-hidden="true" xmlns="http://www.w3.org/2000/svg"
                            fill="none" viewBox="0 0 14 10">
                            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M1 5h12m0 0L9 1m4 4L9 9" />
                        </svg>
                    </a>
                    <!-- Favorite button -->
                    <button class="p-2 group transition-colors duration-200"
                        onclick="addToFavourites(event, '{{this.tmdbId}}')">
                        {{#isMovieInFavourites this.tmdbId ../favouriteMoviesIds}}
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-red-500 group-hover:text-gray-400"
                            fill="currentColor" viewBox="0 0 24 24" id="heart-{{this.tmdbId}}" data-favorite="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                            {{else}}
                            <svg xmlns="http://www.w3.org/2000/svg"
                                class="h-5 w-5 text-gray-400 group-hover:text-red-500" fill="none" stroke="currentColor"
                                viewBox="0 0 24 24" id="heart-{{this.tmdbId}}" data-favorite="false">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                            </svg>
                            {{/isMovieInFavourites}}
                    </button>
                </div>
            </div>
            {{/each}}
            {{/if}}
        </div>
    </div>
</section> --}}

<!-- POPULAR / TRENDING MOVIES -->
<section class="py-16 bg-gray-100">
    <div class="container mx-auto px-4 text-center animate-fadeInUp">
        <h2 class="text-3xl font-bold mb-10">Popular Movies</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8">
            <!-- Example Movie Card -->
            {{#if popularMovies.length}}
            {{#each popularMovies}}
            <a href="/movie/get-movie/{{this.id}}">
                <div
                    class="bg-white rounded-lg overflow-hidden shadow-lg hover:scale-105 transform transition cursor-pointer">
                    <img src="https://image.tmdb.org/t/p/original{{this.poster_path}}" alt="{{this.title}} poster"
                        class="w-full object-cover">
                    <div class="p-4">
                        <h3 class="font-semibold text-lg">{{this.title}}</h3>
                        <p class="text-gray-500 text-sm mt-2">
                            {{#each this.genre_names}}
                            {{this}}{{#unless @last}}, {{/unless}}
                            {{/each}}
                        </p>
                    </div>
                </div>
            </a>

            {{/each}}
            {{/if}}

            <!-- Repeat cards here -->
        </div>
    </div>
</section>

<!-- CATEGORIES / GENRES -->
{{!-- <section class="py-16 bg-white">
    <div class="container mx-auto px-4 text-center animate-fadeInUp">
        <h2 class="text-3xl font-bold mb-10">Categories</h2>
        <div class="flex flex-wrap justify-center gap-4">
            <button class="px-6 py-2 bg-blue-600 text-white rounded-full hover:bg-blue-700 transition">Action</button>
            <button class="px-6 py-2 bg-green-600 text-white rounded-full hover:bg-green-700 transition">Comedy</button>
            <button
                class="px-6 py-2 bg-purple-600 text-white rounded-full hover:bg-purple-700 transition">Drama</button>
            <button class="px-6 py-2 bg-red-600 text-white rounded-full hover:bg-red-700 transition">Sci-Fi</button>
            <!-- Add more -->
        </div>
    </div>
</section> --}}

<!-- Categories / Genres -->
<section class="py-16 bg-gray-900 px-6 text-white">
    <h2 class="text-3xl font-bold text-center mb-10 animate-fadeIn">Browse by Genre</h2>
    <div class="flex flex-wrap justify-center gap-4">
        {{#each genres}}
        <a href="/genre/{{this.id}}"
            class="px-6 py-3 rounded-full bg-white/10 hover:bg-white/20 backdrop-blur-md border border-white/20 transition animate-fadeInUp">{{this.name}}</a>
        {{/each}}
    </div>
</section>

<!-- FEATURED MOVIE SECTION -->
{{!-- <section class="py-16 bg-gray-900 text-white">
    <div class="container mx-auto px-4 flex flex-col md:flex-row items-center animate-fadeInLeft">
        <img src="/images/featured-movie.jpg" alt="Featured" class="w-full md:w-1/2 rounded-lg shadow-lg mb-8 md:mb-0">
        <div class="md:ml-12 text-center md:text-left">
            <h2 class="text-4xl font-bold mb-4">Featured Movie</h2>
            <p class="text-gray-300 mb-6">
                A thrilling adventure that takes you to the edge of your seat.
            </p>
            <button class="px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-full transition">
                Watch Now
            </button>
        </div>
    </div>
</section> --}}

<!-- NEW RELEASES -->
<section class="py-16 bg-gray-100">
    <div class="container mx-auto px-4 text-center animate-fadeInUp">
        <h2 class="text-3xl font-bold mb-10">New Releases</h2>
        <div class="flex overflow-x-auto gap-8 py-4">
            <!-- New Release Cards -->
            {{#each newMovies}}
            <div class="flex-shrink-0 bg-white rounded-lg shadow-lg w-64 hover:scale-105 transform transition">
                <img src="https://image.tmdb.org/t/p/original{{this.poster_path}}" alt="{{this.title}} poster"
                    class="w-full object-cover">
                <div class="p-4">
                    <h3 class="font-semibold text-lg">{{this.title}}</h3>
                </div>
            </div>
            {{/each}}


            <!-- Add more -->
        </div>
    </div>
</section>

<!-- CALL TO ACTION -->
{{!-- <section class="py-16 bg-blue-600 text-white text-center animate-fadeIn">
    <div class="container mx-auto px-4">
        <h2 class="text-4xl font-bold mb-4">Join Us Today!</h2>
        <p class="mb-8">Create an account to save your favorite movies and get recommendations.</p>
        <a href="/signup" class="px-8 py-3 bg-white text-blue-600 font-bold rounded-full hover:bg-gray-100 transition">
            Get Started
        </a>
    </div>
</section> --}}

<!-- ANIMATIONS -->
<style>
    @keyframes fadeIn {
        0% {
            opacity: 0;
            transform: translateY(20px);
        }

        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes fadeInUp {
        0% {
            opacity: 0;
            transform: translateY(20px);
        }

        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes fadeInLeft {
        0% {
            opacity: 0;
            transform: translateX(-20px);
        }

        100% {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @layer utilities {
        .animate-fadeInUp {
            animation: fadeInUp 1s ease-out forwards;
        }

        .animate-fadeInLeft {
            animation: fadeInLeft 1s ease-out forwards;
        }

        .animate-fadeIn {
            animation: fadeIn 1s ease-out forwards;
        }
    }
</style>